#include "main.h"

/**
 * _abs - computes the absolute value of an interger
 *
 * @n: checks for parameters
 *
 * Return: it reurns zero
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (+n);
}
